{
  "name": "areaimagecalc",
  "version": "1.0.0",
  "description": "Measure areas in images and PDFs",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test",
    "serve": "python3 -m http.server 3000"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "jquery": "^3.7.1",
    "vitest": "^2.0.0"
  }
}
